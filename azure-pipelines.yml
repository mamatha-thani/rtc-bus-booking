trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:

- task: Docker@2
  inputs:
    command: 'build'
    Dockerfile: '**/Dockerfile'
    tags: |
      latest

- script: echo "Docker build completed successfully"